# Do never commit your secrets, this one is just for study purposes
# Apply your secrets prior to apply your deployment, services and so on.
apiVersion: v1
kind: Secret
metadata:
  name: mongodb-credentials
type: Opaque
data:
  MONGO_INITDB_ROOT_USERNAME: YWRtaW4=
  MONGO_INITDB_ROOT_PASSWORD: MTIzNA==
